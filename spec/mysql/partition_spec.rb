require 'spec_helper'

describe Mysql::Partition do
  it 'has a version number' do
    expect(Mysql::Partition::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
